import { EditAutoFocusDirective } from './edit-auto-focus.directive';

describe('EditAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EditAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
